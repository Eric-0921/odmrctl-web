[package]
name = "odmr-discover"
version = "0.1.0"
edition = "2021"
description = "Read-only hardware discovery tool for ODMR lab bring-up on macOS."
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
serialport = "4"

# Exclude from the parent workspace so this tool can be built independently.
[workspace]
