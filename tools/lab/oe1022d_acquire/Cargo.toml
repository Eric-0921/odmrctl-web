[package]
name = "oe1022d-acquire"
version = "0.1.0"
edition = "2021"
description = "OE1022D bounded acquisition prototype — raw-first RALL? capture with M2.4 parser."
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
serialport = "4"
odmr-oe1022d = { path = "../../../crates/odmr-oe1022d" }

# Standalone binary — not part of the parent Cargo workspace.
[workspace]
