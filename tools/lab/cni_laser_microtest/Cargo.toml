[package]
name = "cni-laser-microtest"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "M3: CNI Laser low-power microtest (≤5mW, ≤5s) with abort guards"

[dependencies]
clap = { version = "4", features = ["derive"] }
cni_laser_fake_driver = { path = "../cni_laser_fake_driver" }
serialport = "4.9"
scopeguard = "1.2"

[workspace]
