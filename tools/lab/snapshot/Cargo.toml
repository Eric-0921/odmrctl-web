[package]
name = "lab-snapshot"
version = "0.1.0"
edition = "2021"
description = "Read-only real-device station snapshot tool for ODMR lab bring-up."
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }
serialport = "4"

# Exclude from the parent workspace so this tool can be built independently.
[workspace]
