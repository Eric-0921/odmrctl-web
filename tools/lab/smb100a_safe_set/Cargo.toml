[package]
name = "smb100a-safe-set"
version = "0.1.0"
edition = "2021"
description = "Human-approved SMB100A safe-set audit tool for ODMR lab bring-up."
license = "MIT OR Apache-2.0"

[dependencies]
clap = { version = "4", features = ["derive"] }

# Exclude from the parent workspace so this tool can be built independently.
[workspace]
