[package]
name = "cni_laser_fake_driver"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "CNI Laser protocol (binary frame) fake driver for M1 preflight testing"

[dependencies]
