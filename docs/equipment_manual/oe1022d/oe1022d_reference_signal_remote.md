# OE1022D 参考信号 — 配置项 ↔ 远程指令映射(Ch-A + Ch-B)

> **范围**:截图"参考信号配置"标签页(对应说明书第 4.2 节)
> **指令口径**:严格沿用 `05_oe1022d_remote_programming_commands_55_74.md` 原文
> **通道参数 i**:`1` = 通道 A,`2` = 通道 B
> **关键差异**:Ch-A 使用"内部参考",Ch-B 使用"外部参考" — 多个扫频字段在 Ch-B 侧呈灰色禁用

---

## 0. 截图中操作按钮的指令映射

| 截图按钮 | 远程指令 | 通道参数 |
| --- | --- | --- |
| 自动相移 | `APHSD i` | i = 1 (Ch-A) / 2 (Ch-B) |
| 配置参考信号 | 无单独指令(由 5.2.1 的 12 条指令配合完成) | — |

---

## 2.1 参考相位调整

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 参考相位调整(°) | 0.00 (Ch-A & Ch-B) | `PHASD` | `PHASD i,x <lf>` | `PHASD? i <lf>` |

### 参数说明

- `i` = 通道(1 = A, 2 = B)
- `x` = 相位值(实数,单位 °)
- 范围:-180.00° ~ +180.00°
- 精度:0.01°(四舍五入)

**配置示例**:
```text
PHASD 1,-179.0 <lf>    ; Ch-A 设为 -179.00°
PHASD? 1 <lf>          ; 返回 "-179.00"
PHASD 2,90.5 <lf>      ; Ch-B 设为 90.50°
```

---

## 2.2 参考信号源

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 参考信号源 | Ch-A = 内部参考 / Ch-B = 外部参考 | `FMODD` | `FMODD i,j <lf>` | `FMODD? i <lf>` |

### 完整备选值(3 项)

| j | 含义 | 截图对应 |
| ---: | --- | --- |
| 0 | 外部参考(External) | Ch-B 当前值 |
| 1 | 内部参考(Internal) | Ch-A 当前值 |
| 2 | 内部扫频参考(Internal Sweep) | — |

> ⚠️ **说明书补充**(`4.2.2`):还有第 4 个选项 `Channel-A`(仅通道 B 适用,调用通道 A 的参考信号设置),但**远程指令 `FMODD` 未提供对应 j 值**,需通过前面板设置。

---

## 2.3 外部参考信号类型

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 外部参考信号类型 | Ch-A 灰显 / Ch-B = 过零检测 | `RSLPD` | `RSLPD i,j <lf>` | `RSLPD? i <lf>` |

### 完整备选值(2 项)

| j | 含义 | 截图对应 |
| ---: | --- | --- |
| 0 | TTL 上升沿触发 | — |
| 1 | 正弦波过零检测(Sine Zero Detect) | Ch-B 当前值 |

> ⚠️ **说明书补充**(`4.2.3`):前面板还提供 `TTL Neg`(下降沿),但**远程指令 `RSLPD` 仅支持 j=0/1 两值**。
> 当频率低于 1Hz 时,必须使用 TTL 触发方式(j=0)。

---

## 2.4 内部参考信号频率

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 内部参考信号频率(Hz) | Ch-A = 1.000 / Ch-B = 102000.000(灰显) | `FREQD` | `FREQD i,f <lf>` | `FREQD? i <lf>` |

### 参数说明

- `i` = 通道
- `f` = 频率值(单位 Hz)
- 范围:0 ~ 102 kHz(说明书 4.2.4 注明实际可用 1 mHz~102 kHz)
- 最小分辨率:1 mHz
- 默认值:1.000 kHz

> 📝 指令说明:`FREQD? 1` 会返回通道 A **当前的实际参考信号频率**(内部或外部锁相后)。

**配置示例**:
```text
FREQD 1,10E3 <lf>     ; Ch-A 内部频率 = 10 kHz
FREQD? 1 <lf>         ; 返回 "10000.000" 或当前实际频率
```

---

## 2.5 内部扫频类型

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 内部扫频类型 | Ch-A = 线性扫频类型(灰显) / Ch-B 灰显 | `SWTPD` | `SWTPD i,j <lf>` | `SWTPD? i <lf>` |

### 完整备选值(2 项)

| j | 含义 | 截图对应 |
| ---: | --- | --- |
| 0 | Linear(线性扫频) | 截图灰显值(Ch-A 界面默认显示) |
| 1 | Log(对数扫频) | — |

> 截图标注:Ch-A 与 Ch-B 的"内部扫频类型"在 UI 上均呈灰色(Ch-A 因当前参考源 = 内部参考而非内部扫频参考;Ch-B 因当前参考源 = 外部参考)。

---

## 2.6 扫频开始频率

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 扫频开始频率(Hz) | Ch-A = 1000.000(灰显) / Ch-B = 1000.000(灰显) | `SLLMD` | `SLLMD i,f <lf>` | `SLLMD? i <lf>` |

### 参数说明

- `f` = 开始频率(单位 Hz)
- 范围:0 ~ 102 kHz,最小分辨率 1 mHz
- 默认值:1.000 kHz

---

## 2.7 扫频截止频率

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 扫频截止频率(Hz) | Ch-A = 1000.000(灰显) / Ch-B = 0.001(灰显) | `SULMD` | `SULMD i,f <lf>` | `SULMD? i <lf>` |

### 参数说明

- `f` = 截止频率(单位 Hz)
- 范围:0 ~ 102 kHz,最小分辨率 1 mHz
- 默认值:5.000 kHz

---

## 2.8 线性扫频步进频率

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 线性步进频率(Hz) | Ch-A = 1000.000(灰显) / Ch-B = 0.001(灰显) | `SSLLD` | `SSLLD i,f <lf>` | `SSLLD? i <lf>` |

### 参数说明

- 仅在 `SWTPD` 设为 `Linear` 模式(j=0)时生效
- `f` = 步进频率(单位 Hz),范围 0 ~ 102 kHz,最小分辨率 1 mHz

---

## 2.9 对数扫频百分比

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| (Log 模式)步进百分比(%) | 截图中未单独显示(扫频模式下) | `SSLGD` | `SSLGD i,x <lf>` | `SSLGD? i <lf>` |

### 参数说明

- 仅在 `SWTPD` 设为 `Log` 模式(j=1)时生效
- `x` = 百分比数值(单位 %),范围 0~100,最小分辨率 0.001
- 例:x=50 代表 50%

> 截图对应:此项在 UI 上合并显示在"线性步进频率"控件内(根据所选扫频模式自动切换为 Hz 或 %)。

---

## 2.10 扫频步进时间

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 扫频步进时间(ms) | Ch-A = 1(灰显) / Ch-B = 1(灰显) | `STLMD` | `STLMD i,j <lf>` | `STLMD? 1 <lf>` |

### 参数说明

- `j` = 步进时间(单位 ms)
- 范围:1 ms ~ 100 s(100000 ms),最小分辨率 1 ms

---

## 2.11 扫频运行模式

| 截图配置项 | 截图当前值 | 远程指令 | 设置语法 | 查询语法 |
| --- | --- | --- | --- | --- |
| 扫频运行模式 | Ch-A = 停止扫频(灰显) / Ch-B = 停止扫频(灰显) | `SWRMD` | `SWRMD i,j <lf>` | `SWRMD? i <lf>` |

### 完整备选值(3 项)

| j | 含义 | 截图对应 |
| ---: | --- | --- |
| 0 | 扫频停止(Stop) | ✅ 截图当前值 |
| 1 | 单次扫频(Single) | — |
| 2 | 循环扫频(Loop) | — |

---

## 2.12 谐波 1 / 谐波 2(已在输入与滤波器分文件中详述)

| 截图配置项 | 截图当前值 | 远程指令 |
| --- | --- | --- |
| 谐波 1 | 1 (Ch-A & Ch-B) | `HARMD i,1,1` |
| 谐波 2 | 1 (Ch-A & Ch-B) | `HARMD i,2,1` |

> 此项虽然在参考信号界面显示,但**指令**与"输入与滤波器"分文件 1.10 节相同(同一指令 `HARMD`),此处不重复枚举全部备选值。

---

## 附录:本节截图 → 完整指令还原(可直接复制执行)

```text
; ===== Ch-A(内部参考, 1 kHz)=====
FMODD 1,1 <lf>        ; 参考源 = 内部参考
FREQD 1,1.000 <lf>    ; 内部频率 = 1.000 Hz (截图显示,实际默认 1.000 kHz)
PHASD 1,0.00 <lf>     ; 相位 = 0.00°
; 以下为 Ch-A 灰显项(因未选 Internal Sweep),指令无限制可正常下发:
SWTPD 1,0 <lf>        ; 扫频类型 = Linear
SLLMD 1,1000.000 <lf> ; 扫频开始 = 1000.000 Hz
SULMD 1,1000.000 <lf> ; 扫频截止 = 1000.000 Hz
SSLLD 1,1000.000 <lf> ; 步进 = 1000.000 Hz
STLMD 1,1 <lf>        ; 步进时间 = 1 ms
SWRMD 1,0 <lf>        ; 扫频运行 = 停止

; ===== Ch-B(外部参考, 过零检测)=====
FMODD 2,0 <lf>        ; 参考源 = 外部参考
RSLPD 2,1 <lf>        ; 外部类型 = 正弦波过零检测
PHASD 2,0.00 <lf>     ; 相位 = 0.00°
; 以下为 Ch-B 灰显项(因参考源 = External),指令无限制:
SWTPD 2,0 <lf>
SLLMD 2,1000.000 <lf>
SULMD 2,0.001 <lf>
SSLLD 2,0.001 <lf>
STLMD 2,1 <lf>
SWRMD 2,0 <lf>
```

> 注:本节指令出处见 `05_oe1022d_remote_programming_commands_55_74.md` 5.2.1 节"参考与相位指令"。
