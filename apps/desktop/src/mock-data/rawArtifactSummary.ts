import type { RawArtifactSummary } from "./types";

export const rawArtifactSummary: RawArtifactSummary = {
  filename: "oe1022d.rawbin",
  sizeBytes: 3216,
  parsedByGui: false,
  note: "Raw binary parsing belongs to Rust backend / offline parser, not GUI-M0 frontend.",
};
