[package]
name = "odmr-smb100a"
version = "0.1.0"
edition = "2021"
description = "SMB100A RF signal generator — SCPI command catalog and fake device."
license = "MIT OR Apache-2.0"

[dependencies]
odmr-types = { path = "../odmr-types" }
odmr-device = { path = "../odmr-device" }
