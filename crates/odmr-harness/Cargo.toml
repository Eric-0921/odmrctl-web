[package]
name = "odmr-harness"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Test fixtures, fake devices, and mock transports for the ODMR automation system."

[dependencies]
odmr-types = { path = "../odmr-types" }
odmr-device = { path = "../odmr-device" }
