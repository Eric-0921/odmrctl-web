[package]
name = "odmr-mag"
version = "0.1.0"
edition = "2021"
authors = ["ODMR Automation Team"]
description = "Mock-only magnetic field planning, coil matrix computation, and safety validation"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
