[package]
name = "odmr-config"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Canonical JSON configuration models and manual-derived defaults for ODMR stations and runtime."

[dependencies]
odmr-types = { path = "../odmr-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
