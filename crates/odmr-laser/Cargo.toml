[package]
name = "odmr-laser"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "CNI laser Layer 1 serial driver and protocol frames."

[dependencies]
odmr-types = { path = "../odmr-types" }
serialport = "4.9"

